# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
dist/
build/
*.tsbuildinfo

# Jekyll
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
blog/20*/
blog/all-posts.html
blog/posts-preview.html
.bundle/

# Environment
.env
.env.local
.env.*.local

# Agent personal overrides — repo-local but per-developer
CLAUDE.local.md
AGENTS.local.md

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
.obsidian/

# Testing
coverage/
.nyc_output/

# Logs
logs/
*.log

# Temporary files
tmp/
temp/
*.tmp

# OS
Thumbs.db

# Internal docs — session notes, planning, and development docs
# These should never be committed to the public website repo.
# Files already in the repo: use `git rm --cached` to untrack without deleting.
# Note: _config.yml also excludes docs/ from the Jekyll build as defense in depth.
# If public-facing docs are ever added under docs/, update both files intentionally.
docs/
